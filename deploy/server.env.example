# Copy to server/.env and replace placeholders privately on the server.
# URL-encode the password, including $ -> %24 and ? -> %3F.
DATABASE_URL="postgresql://pd_dev_v2:REPLACE_WITH_ENCODED_PASSWORD@paramountdirectdev-v2-db.cjwwc0i804mp.ap-southeast-1.rds.amazonaws.com:5432/postgres?schema=public&sslmode=require"
JWT_SECRET="REPLACE_WITH_RANDOM_SECRET"
JWT_EXPIRES_IN="8h"
NODE_ENV=production
HOST=127.0.0.1
PORT=4000
CORS_ORIGIN="https://pdv2.paramountdirect.com"
# Configure these before using iPeak submission/issuance features.
IPEAK_SERVICE_URL=""
IPEAK_PRIVATE_KEY=""
